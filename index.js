// Your code here

class Cat {
constructor(name, sex){
    speak() {
        `${this.name} says meow`
    }
}
}

class Dog {
constructor(name, sex){
    speak() {
        `${this.name} says woof`
    }
}
}

class Bird {
constructor(name, sex){
    speak() { 
if(this.sex == male){
        `${this.name}, the parrot `
    }
    else 
    `${this.name} says squawk `

}
}
}